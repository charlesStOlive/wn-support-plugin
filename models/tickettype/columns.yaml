# ===================================
#  List Column Definitions
# ===================================

columns:
    name:
        label: waka.support::lang.models.tickettype.name
        searchable: true
    is_facturable:
        label: waka.support::lang.models.tickettype.is_facturable
        type: switch
        searchable: true
    is_for_super_user:
        label: waka.support::lang.models.tickettype.is_for_super_user
        type: switch
        searchable: true
    updated_at:
        label: waka.wutils::lang.global.updated_at
        type: timetense
    sort_order:
        label: waka.wutils::lang.global.sort_order
        invisible: true
